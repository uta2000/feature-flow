name: Check Skill References

on:
  pull_request:
  push:
    branches: [main]

jobs:
  check-references:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Verify skill reference integrity
        run: bash scripts/check-skill-references.sh
